@model IEnumerable<Domain.Models.WorkType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>Qty</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>@item.ProductsInWork.Count</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkTypeId })
        </td>
    </tr>
}

</table>
