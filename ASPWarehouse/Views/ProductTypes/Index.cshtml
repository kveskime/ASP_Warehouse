@model IEnumerable<Domain.Models.ProductType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductTypeDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductTypeDescription.Value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductTypeId })
        </td>
    </tr>
}

</table>
